import { theme } from "../config";

export const CROP_CACHE_PROPS: {
    IMAGE_PATH: string;
    LINE_ONE: string;
    LINE_TWO: string;
    SLUG: string;
} = {
    IMAGE_PATH: `/${theme}/cc-icon.svg`,
    LINE_ONE: "CROP",
    LINE_TWO: "CACHE",
    SLUG: "/cropcache",
};
