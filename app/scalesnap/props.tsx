import { theme } from "../config";

export const SCALE_SNAP_PROPS: {
    IMAGE_PATH: string;
    LINE_ONE: string;
    LINE_TWO: string;
    SLUG: string;
} = {
    IMAGE_PATH: `/${theme}/ss-icon.svg`,
    LINE_ONE: "SCALE",
    LINE_TWO: "SNAP",
    SLUG: "/scalesnap",
};
