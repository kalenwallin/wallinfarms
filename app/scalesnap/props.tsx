import { theme } from "../layout";

export const SNAP_SCALE_PROPS: {
    IMAGE_PATH: string;
    LINE_ONE: string;
    LINE_TWO: string;
    SLUG: string;
} = {
    IMAGE_PATH: `/${theme}/ss-icon.svg`,
    LINE_ONE: "SNAP",
    LINE_TWO: "SCALE",
    SLUG: "/snapscale",
};
